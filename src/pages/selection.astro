---
import Layout from '../layouts/Layout.astro';
import { loadAllQCMs, getMatieres } from '../lib/qcmParser';

// Charger tous les QCM disponibles
const allQCMs = await loadAllQCMs();
const matieres = getMatieres(allQCMs);

// Grouper les QCM par matière
const qcmsByMatiere = matieres.map((matiere) => ({
  matiere,
  qcms: allQCMs.filter((qcm) => qcm.frontmatter.matiere === matiere),
}));

// Icônes et couleurs par matière
const matiereConfig: {
  [key: string]: { icon: string; colorClass: string };
} = {
  Mathématiques: { icon: '🔢', colorClass: 'bg-blue-100 border-blue-300 hover:bg-blue-200' },
  Français: { icon: '📚', colorClass: 'bg-green-100 border-green-300 hover:bg-green-200' },
  Sciences: { icon: '🔬', colorClass: 'bg-purple-100 border-purple-300 hover:bg-purple-200' },
  Histoire: { icon: '🏛️', colorClass: 'bg-orange-100 border-orange-300 hover:bg-orange-200' },
  Géographie: { icon: '🌍', colorClass: 'bg-pink-100 border-pink-300 hover:bg-pink-200' },
  Anglais: { icon: '🇬🇧', colorClass: 'bg-yellow-100 border-yellow-300 hover:bg-yellow-200' },
};

// Mapper la difficulté en étoiles
function getDifficultyStars(difficulte: number): string {
  return '⭐'.repeat(Math.min(difficulte, 5));
}
---

<Layout title="École Interactive - Sélection">
  <div class="mx-auto max-w-6xl">
    <div class="mb-12 text-center">
      <h1 id="page-title" class="mb-4 text-5xl font-bold text-gray-800">
        Quelle matière veux-tu réviser ?
      </h1>
      <p id="page-subtitle" class="text-xl text-gray-600">
        Choisis une matière pour commencer à apprendre !
      </p>
    </div>

    <div class="mb-8 text-center flex gap-4 justify-center">
      <a
        href={`${import.meta.env.BASE_URL}matieres`}
        class="inline-block rounded-xl bg-gray-200 px-6 py-3 text-lg font-semibold text-gray-700 transition-all hover:bg-gray-300"
      >
        ← Retour aux matières
      </a>
      <a
        href={import.meta.env.BASE_URL}
        class="inline-block rounded-xl bg-gray-200 px-6 py-3 text-lg font-semibold text-gray-700 transition-all hover:bg-gray-300"
      >
        👤 Changer d'utilisateur
      </a>
    </div>

    {
      qcmsByMatiere.length === 0 ? (
        <div class="rounded-3xl bg-white p-12 text-center shadow-lg">
          <div class="mb-4 text-6xl">📭</div>
          <h2 class="mb-2 text-3xl font-bold text-gray-800">Aucun QCM disponible</h2>
          <p class="text-xl text-gray-600">
            Les QCM n'ont pas encore été ajoutés. Consultez le README pour ajouter des QCM.
          </p>
        </div>
      ) : (
        <div id="qcms-container" class="space-y-10">
          {
            qcmsByMatiere.map(({ matiere, qcms }) => {
              const config = matiereConfig[matiere] || { icon: '📖', colorClass: 'bg-gray-100 border-gray-300 hover:bg-gray-200' };
              return (
                <div class="matiere-section rounded-3xl bg-white p-8 shadow-lg" data-matiere={matiere}>
                  <div class="mb-6 flex items-center gap-4">
                    <span class="text-5xl">{config.icon}</span>
                    <div>
                      <h2 class="text-3xl font-bold text-gray-800">{matiere}</h2>
                      <p class="text-lg text-gray-600 qcm-count">{qcms.length} QCM disponible{qcms.length > 1 ? 's' : ''}</p>
                    </div>
                  </div>

                  <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3 qcm-grid">
                    {
                      qcms.map((qcm) => (
                        <a
                          href={`${import.meta.env.BASE_URL}qcm/${qcm.frontmatter.id}`}
                          class={`qcm-card block rounded-xl border-4 p-6 transition-all hover:scale-105 active:scale-95 ${config.colorClass}`}
                          data-niveau={qcm.frontmatter.niveau}
                        >
                          <h3 class="mb-2 text-lg font-bold text-gray-800 line-clamp-2">
                            {qcm.frontmatter.titre}
                          </h3>
                          <p class="mb-3 text-sm text-gray-600 line-clamp-2">
                            {qcm.frontmatter.description}
                          </p>
                          <div class="flex flex-wrap gap-2 text-xs">
                            <span class="rounded-full bg-white px-3 py-1 font-semibold text-gray-700">
                              {qcm.frontmatter.niveau}
                            </span>
                            <span class="rounded-full bg-white px-3 py-1 font-semibold text-gray-700">
                              {getDifficultyStars(qcm.frontmatter.difficulte)}
                            </span>
                            <span class="rounded-full bg-white px-3 py-1 font-semibold text-gray-700">
                              {qcm.frontmatter.points} pts
                            </span>
                          </div>
                        </a>
                      ))
                    }
                  </div>
                </div>
              );
            })
          }
        </div>
      )
    }
  </div>

  <script>
    // Filtrage côté client basé sur le paramètre URL et la classe de l'utilisateur
    function filterQCMs() {
      // Récupérer la classe de l'utilisateur depuis localStorage
      const storedUser = localStorage.getItem('ecole_user');
      if (!storedUser) {
        // Si pas d'utilisateur connecté, rediriger vers la page d'accueil
        window.location.href = import.meta.env.BASE_URL;
        return;
      }

      const user = JSON.parse(storedUser);
      const classe = user.classe;

      const urlParams = new URLSearchParams(window.location.search);
      const selectedMatiere = urlParams.get('matiere');

      const pageTitle = document.getElementById('page-title');
      const pageSubtitle = document.getElementById('page-subtitle');
      const matiereSections = document.querySelectorAll('.matiere-section');

      // Filtrer par matière (basé sur l'URL)
      if (selectedMatiere) {
        // Mettre à jour le titre et sous-titre
        if (pageTitle) pageTitle.textContent = `QCM de ${selectedMatiere}`;
        if (pageSubtitle) pageSubtitle.textContent = 'Choisis un QCM pour commencer !';
      }

      // Filtrer les QCMs par classe
      matiereSections.forEach((section) => {
        const matiere = section.getAttribute('data-matiere');
        const shouldShowSection = !selectedMatiere || matiere === selectedMatiere;

        if (shouldShowSection) {
          // Filtrer les QCMs dans cette section par niveau
          const qcmCards = section.querySelectorAll('.qcm-card');
          let visibleCount = 0;

          qcmCards.forEach((card) => {
            const niveau = card.getAttribute('data-niveau');
            if (niveau === classe) {
              (card as HTMLElement).style.display = 'block';
              visibleCount++;
            } else {
              (card as HTMLElement).style.display = 'none';
            }
          });

          // Mettre à jour le compteur de QCMs
          const countElement = section.querySelector('.qcm-count');
          if (countElement) {
            countElement.textContent = `${visibleCount} QCM disponible${visibleCount > 1 ? 's' : ''}`;
          }

          // Afficher ou cacher la section selon qu'elle contient des QCMs pour cette classe
          if (visibleCount > 0) {
            (section as HTMLElement).style.display = 'block';
          } else {
            (section as HTMLElement).style.display = 'none';
          }
        } else {
          (section as HTMLElement).style.display = 'none';
        }
      });

      // Vérifier s'il reste des sections visibles
      const visibleSections = Array.from(matiereSections).filter(
        (section) => (section as HTMLElement).style.display !== 'none'
      );

      if (visibleSections.length === 0) {
        const container = document.getElementById('qcms-container');
        if (container) {
          container.innerHTML = `
            <div class="rounded-3xl bg-white p-12 text-center shadow-lg">
              <div class="mb-4 text-6xl">📭</div>
              <h2 class="mb-2 text-3xl font-bold text-gray-800">Aucun QCM disponible pour ${classe}</h2>
              <p class="text-xl text-gray-600">
                Il n'y a pas de QCM disponibles pour la classe ${classe}${selectedMatiere ? ` en ${selectedMatiere}` : ''}.
              </p>
              <a
                href="${import.meta.env.BASE_URL}matieres"
                class="mt-6 inline-block rounded-xl bg-blue-500 px-6 py-3 text-lg font-semibold text-white transition-all hover:bg-blue-600"
              >
                Retour aux matières
              </a>
            </div>
          `;
        }
      }
    }

    // Exécuter le filtrage au chargement de la page
    filterQCMs();

    // Ré-exécuter si l'URL change (navigation avec le bouton retour/avancer)
    window.addEventListener('popstate', filterQCMs);
  </script>
</Layout>
